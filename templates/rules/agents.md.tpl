# Repository Instructions
Before doing any work in this repository, read and follow `.codexrules` completely.

If `.codexrules` is missing or unreadable, do not modify the repository. Report the missing or unreadable rule file.

Treat `.codexrules` as the repository-specific source of truth. Higher-priority system, developer, and current user instructions still take precedence.
