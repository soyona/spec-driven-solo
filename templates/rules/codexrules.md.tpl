# Codex 项目协作规则（.codexrules）

## 1. 指令优先级与授权边界

1. 系统、平台和开发者指令优先于本文件。
2. 用户当前请求优先于项目历史状态；`memory-bank/` 不得覆盖用户当前授权。
3. 分析、审计、解释、评审和规划请求默认只读。
4. 诊断请求允许执行必要的非破坏性检查，但不自动实施修复。
5. 只有用户明确要求修改、实现或构建时，才允许在请求范围内写入文件。

## 2. 开工读取路由

- 所有项目任务先读取 `memory-bank/projectBrief.md` 和 `memory-bank/techContext.md`。
- 开发任务再读取 `memory-bank/activeContext.md` 和 `memory-bank/progress.md`。
- 涉及数据契约时读取 `memory-bank/dataModels.md`。
- 涉及架构时读取 `memory-bank/systemPatterns.md`。
- 默认禁止读取 `memory-bank/archive/`；只有用户明确要求追溯历史时才可读取。
- 只读取完成任务必需的规则和局部内容，不进行无目标全仓扫描。

## 3. 架构审批门禁

- `memory-bank/techContext.md` 的架构审查状态为 `PENDING` 时，允许读取、分析、审计和提出方案，但禁止安装依赖或修改实现文件。
- 只有状态为 `APPROVED` 且用户当前请求授权实施时，才允许进入修改阶段。
- 不得自行把 `PENDING` 改为 `APPROVED`；该状态由人类架构师确认。
- 规则文件或必要上下文缺失、为空或不可读时，停止写入并准确报告缺失项。

## 4. 项目目录职责

- `product-assets/`：人类提供的产品资产，默认只读；仅在用户明确要求时修改。
- `memory-bank/`：项目上下文与决策记录，按任务读取，满足回写条件时更新。
- `src/`：业务实现源码。
- `tests/`、配置、脚本和文档：任务需要时允许修改，不受“只能修改 `src/`”限制。
- `memory-bank/archive/`：冷归档区，默认不读取、不修改。

## 5. 修改与安全规则

- 修改前检查工作区状态，保留用户已有改动，不覆盖或回滚无关修改。
- 仅修改完成当前请求必需的文件，禁止顺带重构或清理。
- 新增生产依赖前说明必要性；依赖白名单表示允许选择，不表示必须安装。
- 不读取、输出、提交或写入密钥、密码、认证文件和隐私数据。
- 破坏性操作必须确认准确目标并获得明确授权。

## 6. 当前项目绑定轮廓

- **形态/技术栈**：`__PROFILE_NAME__`
- **默认构建指令**：`__COMPILE_CMD__`
- **状态管理策略**：`__STATE_STRATEGY__`

### 准入依赖白名单

`__META_WHITELIST__`

### 依赖黑名单

`__META_BLACKLIST__`

### 工具链约束

__TOOLCHAIN_ROUTER__

## 7. 工作预算与失败处理

- 优先使用精确检索、局部读取和定向验证，不重复读取未变化且已掌握的内容。
- 修改后允许重新读取目标文件以复核结果。
- 已知完整检查必然失败时不重复运行，应说明已知失败边界。
- 只有同一阻塞原因连续重复、没有新证据且无法继续推进时，才停止并请求帮助；不得按不同错误的简单次数机械熔断。
- 浏览器、视觉和真机工具只在用户要求、仓库验收要求或变更确实需要时使用。

## 8. 验证与证据边界

- 修改后优先运行与变更直接相关的定向测试、类型检查或静态检查。
- 最终完整构建最多运行一次。
- 必须区分定向测试、静态检查、完整构建、浏览器验证、真机验证和人工验收。
- 只能报告实际执行并通过的验证，不能把静态或构建结果表述为视觉、真机或人工验收通过。

## 9. Memory Bank 回写

- 只有实现实际改变项目状态、架构决策或进度时才更新 `memory-bank/`。
- 分析、问答和只读审计默认不更新。
- `activeContext.md` 只记录当前仍有效的上下文与决策。
- `progress.md` 只勾选已经完成并验证的事项。
- 不写入完整终端日志、密钥、推测或一次性环境状态。
- 归档属于项目文件修改，必须在当前用户授权范围内执行。

## 10. 完成交付

最终回复应简要说明：

- 修改内容；
- 已执行的验证；
- 未执行的验证及原因；
- 已知风险或人工验收项；
- 是否更新 Memory Bank 及其原因。
